# Delivery status

Updated 2026-09-08 local date. **Goal active; D1, D2, D3A, D4 and D5 local runtime/parity work are merged and cleaned up; invitations remain incomplete.** Provider/hosted deployment readiness remains unverified; no production deployment, paid resource, real invitation or external athlete-data transfer occurred. The [accepted contract](accepted-contract.md), [plan](plan.md), editorial direction and later ADRs govern remaining work.

GitHub: [epic #121](https://github.com/stonematt/bike_race_results/issues/121), [milestone 7](https://github.com/stonematt/bike_race_results/milestone/7). D1: #98/#106 reporting, #100 hook verification, #123 safe setup/runtime. D2: #125 implements the accepted journey and reuses #89/#34/#82. D3: #120/#128. D4: #130. D5: security #124 and runtime/recovery #129. All delivery tickets are native epic children. Unrelated backlog is preserved.

| Increment  | Current state                                                                                                                                                     |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Foundation | Merged through PR #122; cleanup verified below.                                                                                                                   |
| D1         | Merged through PR #126; cleanup verified below.                                                                                                                   |
| D2         | Merged through PR #127; cleanup verified below.                                                                                                                   |
| D3         | D3A merged through PR #131 and cleanup verified. Invitation acceptance remains blocked; D3 is not complete.                                                       |
| D4         | Merged through PR #134; cleanup verified below. Reviewed stories are complete; invitations remain separately incomplete.                                          |
| D5         | Local dependency, runtime, recovery, CI-browser and PGlite/PostgreSQL parity work merged through PRs #132, #136 and #138. Hosted/provider readiness remains #129. |

## Cost-aware continuation after quota exhaustion

The owner reports that delivery exhausted the weekly allowance and authorizes lower-cost models with strategic Astra use. This supersedes the earlier Astra-by-default orchestration recipe. The goal remains unfinished; no new token budget is set.

- Default to GPT-5.6 Terra at low reasoning for bounded implementation, increasing to medium only when needed. Luna may handle mechanical documentation and formatting. Use Astra for a specific architectural impasse or high-risk final review, with a concise evidence packet rather than the entire conversation.
- Keep one implementation worker active by default. Add a second only for clearly independent work that saves elapsed time without duplicating investigation. Do not reactivate old Astra workers for routine work; start fresh bounded contexts with explicit model selection.
- Preserve TDD and independent review requirements. Run the directly affected test lane during changes, then the required integration checks once per candidate; repeat only for changes or failures. Review correction deltas rather than restarting full reviews. Keep tool output and status updates concise.
- No account-quota telemetry or automatic wake/resume control has been established through the available tools. Goal token counts are not remaining weekly allowance. Use the client usage display or CLI /status when available; record checkpoints before stopping when a limit is reported. Do not promise automatic reset detection or resumption.

The earlier quota interruption is resolved for D4. Backend/member reporting is `45b4829`; management UI/action seams are `ebcd0ea`; the race fallback review correction is `21b1361`. One high-risk review found a P2 gap in the no-publication race-review question; its focused red/green regression and correction-delta review are complete. Exact-head CI, merge and branch/worktree cleanup are recorded in the D4 audit below.

D5 runtime is delivered through PR [#136](https://github.com/stonematt/bike_race_results/pull/136), merged into `dev` at `8066a0b8b8823f924254e6a1116933f762a93d7a`; reviewed head `c52486d9bc623a79698e993064d7f452ae04983c`, base `ceda26219bd95dc649ba3c3200b008ee046c4d07`. It provides discriminated native PGlite/node-postgres transports, one app-owned runtime shared by Auth.js and reporting, session-locked same-client PostgreSQL migrations with connection-loss cleanup, isolated native 0007 upgrade proof, and a synthetic Chromium production-browser gate plus [recovery runbook](recovery-runbook.md). High-risk review found three P2 failure-path/proof gaps; correction-delta review cleared them after `1971bd8` and `96b011e`. Exact-head CI [34250019516](https://github.com/stonematt/bike_race_results/actions/runs/34250019516) passed privacy guard, public suite, migration drift, fresh PGlite migration and browser gate. The focused failed-save focus test correction `c52486d` passed 20 repeated isolated runs. Native runtime/Auth/upgrade and membership-lock proofs used fresh loopback-only disposable clusters, all removed with their synthetic credentials.

D5 public reporting and authorization parity is delivered through PR [#138](https://github.com/stonematt/bike_race_results/pull/138), merged into `dev` at `080851048f660212ad6f915f177e01e8c9a388e0`; reviewed head `c617c2cf9df7a2b0fc16f38554e9def0bba4b2a1`, base `d06a6a939188c66ca6deaabb4185c07669e9c509`. The shared synthetic scenarios compare PGlite and PostgreSQL public projections for conference/null-conference denominators, source-bound time-trial provenance, DNF/deficit/null values, checkpoints, saved/cleared squad preference, transaction-entered unavailable-invitation rejection, and next-read invitation/membership revocation. Initial independent review found a required-native-proof P1 plus isolation and public-output gaps; `c617c2c` corrected them, and correction-delta Standards and Spec reviews passed with no findings. Exact-head CI [34253864849](https://github.com/stonematt/bike_race_results/actions/runs/34253864849) passed privacy guard, the public suite with its required loopback-only PostgreSQL service, migration drift, fresh PGlite migration and the synthetic production browser gate. A separate fresh loopback-only local cluster passed the focused shared scenarios and was removed with its synthetic credentials. Cleanup fast-forwarded local `dev` and removed the clean parity worktree and its local/remote feature branch. This proves local transports only: hosted pool attachment, TLS/provider credentials and provider operation remain unverified under #129. No production release, paid resource, real invitation or external athlete-data transfer occurred.

## Current work and resume point

Main checkout is clean on the current `dev` head, including the audited D5 parity merge. The runtime, parity and audit task worktrees and feature refs are gone. Local runtime, recovery, browser and public-domain parity criteria are recorded; provider/hosted deployment readiness remains unverified and must not be inferred from this local work.

D4 foundation, commands and member reporting passed independent Standards and Spec/data/static-art review. The management workspace now exposes only generated evidence, unavailable reasons, revision preview, stale-publication refresh and exact race-review placement; server actions re-check selected season and active club. High-risk review reported no Standards findings and one Spec P2 (the absent race fallback question), corrected at `21b1361` and passed on a delta re-review. Focused candidate lanes pass 10 files / 129 tests before that correction; its RaceReview and protected reporting lane passes 9 tests. Full CI remains pending.

Actual red/green evidence includes member draft denial, draft persistence, revision/review/publication guards, replacement, stale count/source reads, approval-revision validation, exact-Event component placement, incomplete-Round fallback and member route integration. A real archive-to-normalizer test passes: archive-only correction preserves five approved starts; normalization removes the omitted starter, yields a four-start candidate and withholds the old publication. That combined correction characterization first ran green after the earlier source/read red-green cases. PGlite sequential checks do not prove PostgreSQL interleavings.

Synthetic persistent PGlite browser acceptance used `/private/tmp/descenders-d4-browser-resume` with synthetic riders only. Admin development login created a draft, explicitly approved current evidence, published it, reached the protected season placement at `?through=1`, and browser Back returned to the published story; keyboard selection passed and the desktop management capture had no horizontal overflow. The local server and disposable Chrome profile are to be stopped before database writes or migrations. This is local acceptance, not hosted verification.

Prior D3 production and SMTP processes are stopped; port2525 is free. D4 now owns port3100 as recorded above. The production smoke used synthetic persistent `/private/tmp/descenders-d3-browser-final` and a non-relaying, synthetic-recipient-only capture on 127.0.0.1:2525. Browser origin is matching `http://localhost:3100`, CDP 9224 in isolated profile `/private/tmp/descenders-d1-chrome`. The D3 browser fixture has two synthetic clubs with matching Cedar slugs; club 1's user-created Birch keeps slug `maple` and id 4 and is now archived. The earlier `/private/tmp/descenders-d3-browser` preserves the original demo-sequence failure. D2 `/private/tmp/descenders-d2-browser` and original D1 `/private/tmp/descenders-d1-accepted` remain separate. Stop the app before database writes or migrations; never open the same PGlite directory concurrently. No real mail or invitation delivery is configured.

Next: retain #129 for provider/hosted deployment readiness only. Provider-specific pool attachment, TLS/credential configuration and hosted operation require separately authorized provider access; do not create an account, spend money, deploy, send a real invitation or transfer athlete data. Invitation acceptance is still unimplemented after the actual rejection below; explicit local-synthetic-only implementation authorization is pending. Do not bypass it through other work.

## D4 delivery audit

PR [#134](https://github.com/stonematt/bike_race_results/pull/134) merged into `dev` at `aa5fdefc78d0f025220f6ceb426bd0dc46ce8b9f`; reviewed head `c1db6ca6c21915aa5630547ad26dafd855d3f8c2`, base `7876756dcd79255ebe4fd8011feced93e4981af2`. Exact-head CI [34240935582](https://github.com/stonematt/bike_race_results/actions/runs/34240935582) passed typecheck, lint, formatting, 85 public test files / 1,126 passed plus one separate invitation TODO, migration drift and fresh PGlite migrations. The branch’s high-risk review found one Spec P2 in race fallback copy; `21b1361` corrected it with a red/green component and protected-route regression, and a correction-delta review passed. Standards review had no actionable findings.

Focused D4 lanes, local synthetic persistent-PGlite browser acceptance, privacy, and production build passed as recorded above. `brand:check` was skipped in the feature worktree because the private sibling brand repository was unavailable; this is not a token-drift result. The local build retained existing Auth.js/Edge warnings. No hosted database, production release, real invitation or athlete-data transfer was performed.

Cleanup complete: main `dev` fast-forwarded with `--ff-only`; clean D4 worktree `/private/tmp/descenders-d4-stories` and local branch were removed. The remote feature branch needed an explicit post-merge deletion because GitHub could not delete a branch checked out by that worktree; fetch/prune then verified absence. The D5 worktree was retained. Obsidian journal and GitHub delivery record were updated. #130 is complete for the reviewed-story increment; D3 invitation and D5 deployment-readiness work remain open.

## Source-correction prerequisite delivery audit

PR [#133](https://github.com/stonematt/bike_race_results/pull/133) merged into dev at 7876756dcd79255ebe4fd8011feced93e4981af2, reviewed head 51671a9ca1efa219af2d7ff1508b58d01c4e3b75. CI 34214733674 passed that exact head. Independent Standards (d1_demo) and Spec/data (foundation_spec) reviews passed code and final ledger corrections. Main-checkout public suite passed 78 files / 1,025 tests plus the separate invitation TODO; protected local lane 8/85, production build, types, lint, formatting and privacy passed. [Review/merge audit](https://github.com/stonematt/bike_race_results/pull/133#issuecomment-5583664444).

Cleanup complete: main dev updated with --ff-only, source branch removed locally and at origin, refs pruned and verified absent. The clean detached source worktree was removed normally; active stories worktree preserved. Stone-merge run logged. The earlier isolated-worktree suite's sole hook-path assertion failure is retained as a harness limitation; main-checkout verification covered the actual configured hook. Issue #130 remains open: correction is a prerequisite, not published stories.

## D5A dependency delivery audit

PR [#132](https://github.com/stonematt/bike_race_results/pull/132) merged into dev at `1fcd49018c1495ad3d74bcde052efdc22dfb2393`, reviewed head `0705d0f087c1fa5dc4c8c748822d002057b993d0`. CI `34213204895` passed that exact head. Independent Standards and Spec reviews passed the dependency and final documentation diff with no remaining findings. Local audit, public/local suites, build and production synthetic-mailbox evidence are recorded below and in [dependency readiness](dependency-readiness.md). [Merge audit](https://github.com/stonematt/bike_race_results/pull/132#issuecomment-5583358730).

Cleanup complete: main dev updated with `--ff-only`, dependency branch deleted locally and at origin, fetch/prune and actual remote checks confirmed absence. Stone-merge run logged. The active source-correction worktree was preserved. Issue #124 remains open for upgrade/parity; no production release. GitHub parsed a negated closing phrase as an issue-closing reference, so the PR body was corrected and closing references verified empty before merge.

## D3A delivery audit

PR [#131](https://github.com/stonematt/bike_race_results/pull/131) merged into dev at `76f583574e10868dbc27c1e4b8f3882b2444823b`; reviewed head `ab50c0ce6ed71c9e0736cfaf77f1e8ffcfc9e93b`, base `86b1b57d08eb4b4d04d9ff6aa58691d7e8900897`. Full partitioned Standards and Spec review plus exact correction/documentation reviews passed. CI `34211576812` passed that exact head, including 78 public files / 1,022 tests and one explicit acceptance TODO. Final local production build, type/lint/format/privacy, synthetic navigation and prior scoped art/auth/persistence checks passed as recorded below. Review/merge audit: [PR comment](https://github.com/stonematt/bike_race_results/pull/131#issuecomment-5583013481).

Cleanup complete: clean dev updated with `--ff-only`, task branch deleted locally and at origin, fetch/prune and actual remote-ref checks confirmed absence. Only the main worktree remained; no other worktree was removed. Stone-merge run logged successfully. #120/#128 remain open because D3 is partial; no production release. Obsidian integration milestone recorded through CLI. The dependency branch was created only after cleanup.

## D2 delivery audit

PR [#127](https://github.com/stonematt/bike_race_results/pull/127) merged into dev at `86b1b57d08eb4b4d04d9ff6aa58691d7e8900897`; reviewed head `4e7bbcfe008d796e57534b00b456a7fe128c9532`. Standards and Spec review cover full D2 through 472e19f plus the final correction delta; all three Spec findings closed. CI `34201716036` passed the exact head: 69 public files / 958 tests, privacy, types, lint, formatting, migration drift and fresh PGlite migrations. Final production rebuild passed. Prior local auth smoke, protected 85-test lane, brand and synthetic browser evidence retain the limits below.

Cleanup verified: clean local dev fast-forwarded to the merge, task branch deleted locally and at origin, refs pruned and absent, one main worktree remained. #125/#34 closed, no stale staged labels; #89/#82 retain follow-up scope. Stone-merge log recorded review/CI/cleanup with no classifier denial. Epic D2 and Obsidian milestone updated; no production release. D3 branch was then created from that clean dev.

## D3 activation

#128 is the distinct membership/invitation ticket; #120 reuses squad authority/persistence work. Both are native children of #121, in milestone 7 and ready for implementation. #120's historical grilling blocker is superseded by ADR-0005 and the accepted contract; reconciliation is recorded in its comments. ADR-0007 records technical defaults within that approval. Real invitations, paid resources and external athlete-data transfer remain excluded.

Ordering history from actual automatic review: the first Edge admission transition was rejected before Node membership guards existed because it would create an authorization gap; no runtime patch was applied. The safe sequence installed schema, Node membership reads and every protected route/action guard first. Only after that concrete coverage existed did the narrower reviewed transition replace allowlist authority with supported-provider provenance at Edge plus active database membership at Node. Legacy coach/singleton fallback was removed from the remaining public race-detail query. Auth/admission/access/race focused public tests passed (69), as did the protected local lane (85). Production replay verification remains in the current D3A evidence below. This resolved sequencing does not bypass the separate invitation-acceptance rejection.

## D1 delivery audit

PR [#126](https://github.com/stonematt/bike_race_results/pull/126) merged into dev with merge commit `4ad144a72aeaf08bb5d535bbf201bb09cf4ed998`; reviewed head `38824bc2f3d2fcce7ebf6258eab77e24993428e5`. CI run `34192422103` passed, including 62 public test files / 901 tests. Independent review coverage and resolved findings appear below and in the PR.

Cleanup confirmed: local dev fast-forwarded, reviewed head ancestry verified, local and remote `feat/local-delivery-foundation` deleted, refs pruned and absent. One clean main worktree remained on dev before creating D2 branch. Issues #98/#100/#106/#123 closed, no stale staged labels. Stone-merge run log recorded the result. Epic #121 D1 checkbox and Obsidian journal updated; D2–D5 remain incomplete.

## D2 first slices

- Query contract: explicit checkpoint through ordinal or none. Default latest published ordinal; no published results retains an unpublished schedule. Distinct club riders supply starts, never sums of squads. History preserves all result rows per race; selected event disambiguates. Links retain checkpoint.
- Parent current-season regression ran red: explicit missing 2027 returned 2026. Optional CURRENT_SEASON now prevents that fallback; malformed settings reject. Existing/default lookup remains latest recorded year. Focused season query suite passes 23 tests. Committed as `c073526`. Root empty state offers deliberate recorded-season links rather than seed commands. Authenticated browser check passed: explicit 2027 stays empty at root; 2026 requires its recorded-season link. Test server stopped.
- Dispatch query now suppresses explicit-none data, validates recorded checkpoints, counts distinct club-season riders and treats incomplete conference results as partial with unknown counts. Numerical red/green evidence covers explicit-none false zeros and partial-result false zeros. Initial API red was a missing export; some subsequent characterization cases first ran green.
- First SeasonDispatch passed independent art and data review after correcting unsupported narrative, null-to-zero, mobile race visibility, partial-result fallback, spacing and club-specific copy. Seven initial and four confirmation desktop/mobile captures were inspected; no overflow. Captures are `/private/tmp/descenders-d2-*.png`. This is bounded dispatch acceptance, not full D2 acceptance.
- Keyboard checkpoint selection/submission passed using trusted CDP typeahead, Tab and Enter: Race 2 → Race 3 and `/2025?through=3`. Harness `/private/tmp/descenders-d2-keyboard-typeahead.mjs` assigns no values or synthetic DOM events. Initial focus was programmatic. The original Arrow/Enter CDP sequence failed; its native-popup cause remains unproven.
- Race category query passed independent analyst review: exact event/category/conference fields, club-season highlights, no squad duplication, source status and unknown values preserved. RaceReview and route integration are implemented and reviewed below.
- Rider query: independent analyst PASS after sparse source-slot labels and derived gap provenance corrections. Official totals remain separate from none/one/many measured splits. Unreadable split remains raw with seconds null. Initial missing history, missing neighbors, unreadable-zero and sparse-slot cases ran red; additional guards first ran green.
- Roster query: independent analyst PASS, exact club/season and optional squad scope, includes unassigned/no-result members, preserves every split-event result, checkpoint bounds category grouping. One set-based result read follows the shared dispatch read, not one query per rider. Latest-category ties use source-event-ID order and retain all evidence. Four focused roster tests; combined query suites 26/26.
- Season/roster UI: final independent art PASS after two bounded capture rounds. Sticky rider and category labels survive horizontal table scroll; outer pages have no overflow. Club/squad controls retain checkpoint. Actual UI red/green cases cover lost squad checkpoint, missing roster navigation, result-availability distinctions, split-result preservation/deduplicated participation and unavailable schedule. Parent component/navigation lane 18/18. Static More from this weekend review link and club-roster entry accepted; D4 story publishing remains separate.
- Trusted-click synthetic browser journey passed: dispatch → squad → club → named rider → exact event → Field context → race review, retaining through=2. Harness `/private/tmp/descenders-d2-journey.mjs`; final URL Race 1 with through=2 and exact source anchor. First harness timeout wrongly searched visible text for an aria-label; corrected harness passed. The later GET perspective and Back recheck is recorded below.
- Race: independent art PASS after two bounded capture rounds and static count/singular/DNF copy closure. Lead/partial coverage, one shared strip, named callouts, singleton and noncomparable states accepted. No page overflow.
- Rider: independent art and analyst code PASS after one combined correction batch and static selected-view/deficit corrections. My ride and Field context are bookmarkable GET perspectives with one visible panel and a visible selected marker. Selected official evidence precedes compact history; missing values, source-slot labels, DNF/lap deficits, field qualifiers and rounded gaps relative to this rider are explicit. Meaningful DNF guard regression was confirmed red by removing the guard, then restored green (6 rider tests). Two visual rounds, no overflow; no third round needed for copy/classes.
- Final Impeccable detector returned `[]`; no plugin initialization/update or additional capture loop. This supports the separate art/data gates rather than replacing them.
- Integrated checks: full public suite 69 files / 956 tests passed before the final DNF regression addition. Final rider lane 6/6 and legacy language lane 52/52 passed afterward. Types/lint/repository formatting passed; Final-head CI later passed 958 tests, recorded above. Protected local lane 8 files / 85 passed without refetch or identifying output. All 15 brand tokens match. Privacy guard clean across 294 tracked files.
- Production build passed. Local production startup, anonymous redirect, retained development-session rejection, development provider disabled despite AUTH_DEV_LOGIN=1, and no-referrer passed. This does not establish hosted-driver or production email readiness (D5).
- Corrected browser journey passed actual GET perspective switch and native browser Back, then exact-source race review with through=2. Direct future/invalid checkpoint URLs returned 404; legacy event URL retained year, checkpoint and source anchor. Scripts `/private/tmp/descenders-d2-{journey,direct}.mjs`; all task app servers are now stopped.
- D2 commits: `c073526` current-season selection, `5fbaf73` D1 cleanup activation ledger, `a7178b1` reviewed editorial queries, `5bb136a` season/roster navigation, `f94d645` honest legacy descriptions, `019e37e` race/rider views. PR [#127](https://github.com/stonematt/bike_race_results/pull/127) subsequently merged; exact review/CI/cleanup facts are recorded above.

### D2 final code review

Independent review of `4ad144a...472e19f`: Standards 0 findings; Spec 3 P2 findings. Correction `4e7bbcf` resolved all three with red/green regressions: roster slug/choice reads receive resolved club scope (24 tests); included partial races link their review; one comparable finisher retains outside DNF without an invented spread (58 affected UI consumer tests). Independent Standards and Spec supplement reviews passed the exact correction delta. Final CI `34201716036` passed head `4e7bbcfe008d796e57534b00b456a7fe128c9532` before merge; see D2 delivery audit above. Review trail: [initial findings](https://github.com/stonematt/bike_race_results/pull/127#issuecomment-5581315868), [closure](https://github.com/stonematt/bike_race_results/pull/127#issuecomment-5581380257).

**Foundation delivery audit**

- PR [#122](https://github.com/stonematt/bike_race_results/pull/122), base `dev` at `db1298edd7f6365e55e7ee48282251fc0adc58db`, reviewed head `d3a907230c24052fad9b3517aab8a9225f8bca3a` (the two original commits preserved).
- Independent Standards reviewer `foundation_standards`: zero findings. Independent Spec reviewer `foundation_spec`: zero blockers; activation follow-ups are addressed by the accepted contract and current ledger. Review recorded in the PR body.
- Local Prettier, whitespace and privacy guard passed (264 tracked files). CI run `34188391091` passed privacy, types, lint, formatting, public tests, migration drift and fresh PGlite migrations. Documentation validation does not establish runtime product acceptance.
- Merged into dev with merge commit `36ab935e7c5a8f79e88fa75dd2581499fa65060b` on 2026-09-07 local date. Local dev fast-forwarded; task tip ancestry verified; task branch removed locally and at origin; fetch/prune and both local/remote ref checks confirmed absence. No task worktrees existed to remove.
- Epic #121 and map #1 reconciled in place; milestone 7 remains open. Existing backlog/milestones preserved, no implementation issues closed. Obsidian `tyee/2a/scd/Descenders race reporting.md` updated through CLI.

## D1 implementation and evidence

- `15cf1d3`: accepted product defaults/public TDD seams activated in canonical docs.
- `32607f2`: auth and reporting share persistent identity; lazy auth configuration avoids build-time PGlite startup. Adapter identity and initialization regressions were observed red, then green.
- `85ec60d`: reproduced #100 equivalent absolute hooksPath failure; installed hook now verified by rejecting a forced synthetic payload. Personal Git configuration unchanged.
- `6c67746`: synthetic demo command with 2026 current season, 2025 checkpoint and two squads; no private corpus/config/name-map input. Reopen/rerun tests passed. Independent review subsequently found migration occurred before unrelated-database refusal; corrected in `0b09ca3` with refusal before migration, recognized older-demo upgrade, and command-level legacy/public/custom-schema regressions. Only the exact Drizzle migration metadata table is exempt.
- `6ec5a89`: conference-specific denominators, combined State Champs field and exact source revision binding; ADR-0006 records provenance. Merely archiving a later/hidden alternative cannot reclassify normalized rows; legacy missing provenance fails closed until normalization.
- `f57ba67`: matching browser/AUTH_URL guidance and explicit application build-tracing root.
- `080564c`: production rejects a development-provider token even when its email is allowlisted. Browser smoke reproduced the defect; the regression was run red before the fix. Email-provider allowlist behavior remains valid.
- `c9ce385`: sole selected hidden TT source follows the bound-source contract. Public archive → normalize → reporting regression ran red (place instead of 10% back), then green. Independent analyst re-review passed with zero findings; unselected archives remain inert.

| Check                     | Evidence and limits                                                                                                                                                                                                              |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Public suite              | 62 files / 900 tests passed after c9ce385 and the initial preflight correction. The final additional Drizzle-schema refusal regression and demo/URL lane passed (12 tests); final-head CI passed (901 tests), recorded above.    |
| Protected local corpus    | Full configured local lane passed: eight files / 85 tests after c9ce385. No refetch or identifying output published.                                                                                                             |
| Types/lint/format/privacy | Final correction typecheck passed. Integrated lint, formatting, whitespace and privacy passed (277 tracked files).                                                                                                               |
| Brand                     | All 15 tokens matched; check was not skipped. D1 adds no visual redesign.                                                                                                                                                        |
| Production build          | Passed after080564c, without earlier eager-PGlite aborts.                                                                                                                                                                        |
| Production browser        | Retained original development cookie rejected after080564c; anonymous redirect, provider disabled despite AUTH_DEV_LOGIN=1, and no-referrer passed.                                                                              |
| Development browser       | Fresh final-migration database: synthetic login → Cedar → Summit → Race 1 → direct category → Back passed. Opened 1440px desktop and 390px mobile captures with no overflow. Existing D1 UI only, not D2 editorial acceptance.   |
| Migrations                | Fresh migrations and non-mutating drift check passed after c9ce385. Disposable 0000–0004 → 0006 upgrade/reopen preserved identity, current season, squad and reporting on the final migration bytes. Owner's database untouched. |

TDD limits: conference denominator, missing-layout percentage, auth persistence/startup and dev-token regressions ran red first. Initial source-binding tests followed implementation after an independent failing analyst reproduction; do not claim strict red-first development for that portion. Subsequent sole-hidden-source correction did run red first.

## Review audit

- Independent Standards and Spec reviewed base 36ab935 through 080564c. Both found the demo migration-before-refusal defect; Spec also found sole-hidden-TT mismatch. Do not treat these reviews as unconditional approval.
- Analyst re-review passed the c9ce385 correction. Independent Standards reviewer passed exact c9ce385...0b09ca3342934b55689c72f9ae992251cae65456 demo delta with zero remaining Standards/Spec findings.
- Spec reviewer authored the earlier hook correction 85ec60d; separate Standards reviewer independently checked that commit against #100 and passed with zero findings. The remaining Spec review was independent.
- No human GitHub approval is replaced by these agent reviews. Current diff/head, review state, findings and checks must be rechecked before merge.

## Decisions and readiness investigations

The owner activated delivery on 2026-09-07, approving product defaults/public seams subject to later decisions. Persistent administration and reviewed editorial writes narrowly supersede the historical plate-attachment-only rule; no private behavioral notes or discussion logs are authorized. Art direction and data analysis govern reporting acceptance. See [readiness notes](readiness-notes.md) for D2 composition and D5 transport research.

Browser diagnosis preserved privacy headers: programmatic requestSubmit produced Origin:null; trusted post-hydration mouse submission succeeds. The actual sign-in cookie bounce came from mismatched AUTH_URL/browser hosts. Use one matching origin. Production smoke then exposed the separate dev-token replay defect fixed above.

Registry audit history: initial network escalation was rejected because dependency metadata transfer to npm lacked destination-specific authorization. New read-only evidence proved the repository public and the local lockfile identical to public dev (Git blob e4d767df6bb306dd5bd1123ac9e09259bed6d518). The same audit was subsequently approved and completed; no alternate transfer or bypass was used. The initial approval blocker is resolved.

Audit reports 13 advisory findings (3 critical, 5 high, 5 moderate), not proof of exploitation. Auth.js, Drizzle and PostCSS targeted compatibility/security updates are required under #124 before readiness. Local evidence: `/private/tmp/descenders-prod-audit.json`. No athlete data or secrets transferred.

PostgreSQL 17.11 installed via Homebrew for disposable synthetic parity. Homebrew initialized its empty default cluster at `/opt/homebrew/var/postgresql@17`; no service started. Use a separate task-owned temporary cluster, not that default. Standard node-postgres pool/Drizzle transport is the proposed implementation; no hosted provider is verified.

Obsidian journal: `tyee/2a/scd/Descenders race reporting.md`, maintained exclusively through the CLI. Updated at 080564c checkpoint with foundation merge, D1 evidence, auth discovery, advisory work and remaining increments. D1 merge has also been recorded; refresh at the next integrated milestone. Canonical implementation state stays in this repository.

Documentation accuracy/scope review by `foundation_spec` passed after correcting final migration status and advisory terminology; final PR/CI facts are recorded in #126 before merge.

### D3 request-time authority and operations progress — 2026-09-08

Migration 0007 is generated and drift-free. The data owner's 71-test seed/migration lane passes, including legacy-user-only coach backfill, explicit first-admin bootstrap and managed squad/archive preservation. Parent squad commands have eight passing migrated synthetic tests covering create, stable rename, current actor denial, archive/read-only behavior, multi-squad roster changes and rollback, navigation-only preferences and admin-only account assignment. New public command stubs supplied the initial red evidence; the denial and forged-roster regression cases first ran green against the preceding guarded implementation. Management reads have two passing synthetic tests for role-limited metadata and exact club/season choices. These are in-progress local checks, not D3 acceptance or deployment verification.

A second automatic approval review rejected the invitation acceptance implementation before applying it: “This adds production invitation-acceptance logic that grants authenticated users club roles and mutates membership/audit state, while the user explicitly reserved real invitations for separate authorization.” The parent disclosed the rejection and asked for explicit authorization to implement and test acceptance using local synthetic accounts without sending real invitations. Acceptance remains unimplemented pending that answer; no alternate tool or indirect membership-grant path will bypass the rejection. Unaffected squad operations, guarded reads and reporting/navigation work continue.

### D3A verification and review corrections

The integrated public run passed 77 files / 1,010 tests with one explicit invitation-acceptance TODO before the final three demo-sequence regressions and managed-admin-assignment regression were added. Those final focused lanes pass: demo 14/14 and seed 70/70. A fresh full run is required before integration. Typecheck, lint, formatting, production build and all 15 brand tokens pass. The prior privacy check covered the then-tracked files; staging/new-file privacy and final PR CI remain required.

The demo first reproduced a real `squad_pkey` collision after bootstrap because explicit fixture IDs had not advanced serials. A public bootstrap→createSquad regression ran red, then green. Known old-0006 upgrade with stale sequences also ran red then green; exact current rerun is covered. Sequences align only after the exact safe-demo checks, and managed/unknown databases still refuse before changes. Parent precommit review additionally reproduced `seedAdmin` restoring a removed account assignment. It now links active squads only when that invocation establishes the first active admin; ordinary reseed and revoked memberships preserve managed state.

Independent UX review found stale cross-tab club context and disappearing archive feedback. Real-database server-action and wrong-season command regressions reproduced the context defects and now pass. Forms bind the displayed club and season, re-read active authority, and reject a changed club selection. Archive redirects to a persistent, focused confirmation. Assignment copy now describes choices and coach authority accurately. These review findings are corrected, not waived.

Browser evidence uses synthetic data only. Create→rename with stable slug→duplicate-name failure retaining value/focus→roster save→preferred squad→saved route passed. After full shutdown and reopen, the saved preference and renamed squad remained accessible; the safe demo command refused the managed database. Two-club same-slug navigation, stale cross-tab create rejection, archive confirmation/focus, retained read-only URL and removal from operations/dispatch active choices and the saved preference passed. The legacy roster switcher and category default were not covered by that browser check; the subsequent independent review found their remaining assigned-only/alphabetical behavior. The harness was corrected for the intentionally preserved checkpoint query, native radio chooser selectors and background-tab frame throttling; those were test-harness corrections, not app defects. The browser did find banner overflow and low-contrast links; wrapping and link color were fixed before final captures.

Independent art review passed the desktop/mobile overview and the final expanded-form/error/archive/read-only batch. The reviewer opened the captures; parent ran the interactions. Impeccable detector returned no findings for the new operations components. Art acceptance applies to the D3A squad slice only; invitation UI is not implemented or accepted. The subsequent existing-account management sub-scope passed its own independent visual review.

Production smoke currently confirms the retained development session is refused, the dev provider stays absent despite AUTH_DEV_LOGIN=1, and an email-provider sign-in request for the already-active synthetic admin reaches only the local capture. Token consumption succeeded for the existing synthetic admin, with nodemailer session provenance, no-store and no-referrer. Reusing that token with browser cookies cleared was rejected and created no session. The harness originally expected /signin for replay failure; it was corrected to Auth.js’s actual /api/auth/error?error=Verification route. These checks precede the planned D5 dependency upgrades and Postgres transport and must be repeated there. No invitation token was accepted, no account membership granted by that smoke and no real recipient contacted.

Existing active-account management is now implemented and locally accepted within D3A. Admin-only role change and revocation serialize on the club row, re-read actor/subject membership, protect the last active admin and audit successful mutations. They never insert or reactivate memberships and never consume invitation tokens; the separate acceptance rejection remains pending. Public role/revoke command reds and the last-admin behavioral red now pass; further shared-guard characterizations first ran green. The real-database server-action test confirms the last-admin error is translated to “Assign another active admin first.” without persisting demotion.

Synthetic browser checks passed: members see no management controls; sole-admin demotion is denied; an existing member can become coach; revocation requires confirmation and yields a persistent focused notice. Restoring the member's cookie captured before revocation is denied on the next season and direct-squad request. Independent art review opened the account-management, revoked-row and access-unavailable captures and passed them. No invitation or reactivation behavior is implied. All app/mail servers are again stopped.

D3A implementation commit: `0d882fb` (Conventional Commit with hooks, explicit staging). Final pre-review public suite: **77 files / 1,024 passed / one explicit invitation-acceptance TODO**. Production build passed after the account UI was included. Typecheck, lint, formatting, whitespace, no-schema-drift generation, fresh PGlite migration and staged/new-file privacy checks pass (318 tracked files). Brand remains 15 matching tokens; protected local lane remains 85 passing tests from the auth/query correction, with no later local-corpus behavior changes. Logs: `/private/tmp/descenders-d3a-public.log`, `/private/tmp/descenders-d3a-build.log`. A reviewed PR and exact-head CI are still required; these local results are not a merge or D3 completion.

Independent pre-PR review at `86b1b57...3b63690`: Standards found the legacy roster switcher still used assigned squads, including archived assignments; Spec also found the category page retained an alphabetical default instead of the shared active preference resolution. Supplemental Standards found a non-null assertion in the shared navigation resolver. Correction `8e78c2f` closes these findings. Both independent Standards (`d1_reporting`) and Spec (`foundation_spec`) passed the exact working delta before commit. Five public migrated-database route cases replace seven obsolete alphabetical-helper tests; two cases ran red then green, and the remaining shared-behavior cases first ran green. Bootstrap assertions now verify explicit choice. Documentation correction `d24040c` distinguishes bootstrap allowlisting from request-time authority and keeps the invitation gap explicit; both reviewers passed it.

Final correction verification: **78 public files / 1,022 passed / one invitation-acceptance TODO**; production build and lint pass. Focused route/query/seed/demo lane passed 136 tests; typecheck, formatting and whitespace passed. Logs: `/private/tmp/descenders-d3a-correction-public.log` and `/private/tmp/descenders-d3a-correction-build.log`. Exact-head PR CI remains required.

D4 proposal revision 2 now includes season and exact-Event race placements, selected-list/visibility provenance, current-roster distinct counts, coherent review snapshots and complete editorial journeys after independent art/data direction. It is a proposal, not shipped functionality. Source inspection identified that normalization upserts can retain a row omitted by a correction; D4 must prove corrected coverage before approving counts. GitHub [#130](https://github.com/stonematt/bike_race_results/issues/130) tracks the bounded workflow; [#129](https://github.com/stonematt/bike_race_results/issues/129) tracks PostgreSQL runtime and recovery readiness alongside security issue #124. Both are native children of epic #121 in milestone 7, with explicit dependency links. Obsidian current state and D3A evidence were refreshed through the CLI.

The final synthetic browser recheck passed the corrected `/2026/roster` active choices, archived-choice exclusion and retained archived direct URL. The temporary harness needed its established post-hydration sign-in wait and the actual `/roster` route instead of `/club`; no application change followed those harness corrections. Script: `/private/tmp/descenders-d3-nav-correction.mjs`. Task app server stopped afterward; SMTP remains stopped.

### D5A dependency verification — 2026-09-08

Before PR #132 merged, dependency verification ran on `fix/runtime-dependency-advisories` from dev `76f5835`. `d1_demo` updated Auth.js beta.32/core 0.41.3, Drizzle ORM 0.45.2/Kit 0.31.10 and nested PostCSS 8.5.28. The original production audit's 13 findings now resolve to zero. The initial Nodemailer 8.0.11 choice introduced a newer high advisory; patched 9.1.1 is retained, with the upstream Auth.js peer-range mismatch visible. No metadata override conceals it. [Dependency readiness](dependency-readiness.md) records sources and verification limits.

Full public lane passed 78 files / 1,022 tests plus the separate invitation TODO; protected local lane passed 8 / 85. Production build, typecheck, lint, format, privacy, drift and fresh PGlite migration passed. Actual production-mode local synthetic mailbox login, consumed-link rejection with cleared cookies, development-provider/session refusal and private response headers passed after the updates. App and SMTP are stopped. Local compatibility is verified; declared upstream Nodemailer support and hosted-provider verification are not inferred.

Independent Standards and Spec reviews passed the frozen implementation with zero code findings. Documentation review corrected stale resume wording and explicitly retained #124’s upgrade/parity criteria. Exact-head CI subsequently passed and the branch merged; see the D5A delivery audit above. Parent owns these records and integration. In parallel, `d1_reporting` owns an isolated source-correction prerequisite under #130: worktree `/private/tmp/descenders-d4-source-fix`, branch `fix/normalized-source-corrections` from dev `76f5835`. It may contain active edits; do not remove or switch that worktree during D5A cleanup. No source-correction implementation is included in D5A yet.

### D4 source-correction prerequisite

Commit `d62ebc9` makes the selected individual-result set source-authoritative within one Event: upsert retained rows, delete omitted plates, then update the source binding in the same existing transaction. It preserves raw archives, athlete mappings and unrelated Events; other source families retain their existing upsert semantics. A permitted empty selected list removes that Event’s result field.

The public archive→normalize→race-category regression reproduced the omitted rider remaining in a two-rider field, then passed after the correction. Focused normalization suite passes 23 tests; later sibling-event, rerun, failed-decode preservation and empty-list characterizations first ran green. Failed decoding preserves before writes; that check is not evidence of a failure after partial writes or PostgreSQL concurrency. Independent Standards and Spec/data reviews passed, including the final header clarification in Standards. The isolated branch then merged updated dev without conflicts; the patched-graph production build, types, lint, formatting, privacy and local corpus (8 files / 85 tests) pass. The worktree public run passed 1,024 tests with one acceptance TODO but failed the existing hook-installation path assertion: Git uses the configured absolute main-checkout hook, while the assertion expects this worktree’s hook path. No hook or personal Git setting was changed to satisfy it. Integration will move to the main checkout for the final complete public run; this worktree run is not recorded as passing. The worktree uses the ignored existing local-corpus symlink for fidelity checks, with no refetch or public output.

Final source verification in the main checkout passed **78 public files / 1,025 tests / one separate invitation TODO**. The installed-hook assertion passes with the actual main-checkout configuration; no personal hook setting changed. The earlier isolated failure remains recorded above. Production build and local corpus passed in the isolated checkout on the same patched code/dependency graph. Logs: `/private/tmp/descenders-source-{public,main-public,local,build,format}.log`.

While source integration proceeds, `d4_evidence` owns a bounded new evidence-query slice in `/private/tmp/descenders-d4-stories`, branch `feat/reviewed-stories` from current dev `1fcd490`. Only candidate-query/public synthetic test files are assigned; no schema/UI yet. This worktree may be dirty and must be preserved during source-task cleanup. It will merge the source prerequisite through updated dev before full D4 integration. No invitation acceptance is part of this task.

### Login + Season increment — 2026-09-09 (checkpoint preparation)

Current owner disposition: Team Kit Pop won the team vote. The owner accepts every current Season visualization for delivery to `dev`, including Season pulse, the headline and highlight strip. Future design refinements do not block this milestone. The authorized next deliverable is discrete checkpoint commits, branded TopNav with a removable upper-left QA panel, independent review and a pushed PR to `dev` (#146; Refs #143/#144/#145). TopNav and PR evidence remain outstanding at this checkpoint. No production release is authorized.

The worktree was synchronized without conflicts to `origin/dev` at `3292d95769325c3a98d697824db796d54ce39464`. All 16 previously reviewed source/document hashes were preserved; `.gitignore` combines upstream exclusions with the local-art exclusion. Post-sync typecheck and 29 affected tests passed. This feature changes no schema, migrations, seeds or ingestion logic and has not connected to Neon. Prior review coverage is reused for unchanged source; new navigation requires fresh review and browser checks.

Historical implementation/UAT evidence below predates that owner disposition. Its pending-acceptance and no-delivery statements describe those earlier checks, not the current delivery authorization.

Final spacing follow-up: at owner's request, a bounded Impeccable review produced only CSS refinements (wider mobile schedule cards, single-row tablet ribbon, pulse border fitted to grid). Desktop/mobile/tablet geometry and keyboard/overflow checks pass, as do formatting/whitespace and focused independent Standards/Spec reviews. Final Season owner acceptance remains pending morning UAT; no later-page or delivery action.

Owner UAT update: login accepted locally (“login is good”); Season remains pending after category/schedule refinements. The pulse now displays compact published-category labels and ascending category/name order. The ribbon adds sourced 2026 Oregon dates with explicit conference distinctions, preserves extra available result rounds, and keeps scheduled races out of participation denominators. No login, database or later-page changes. The initial affected lane passed 10 tests; the added extra-round regression's schedule lane passed 3/3. Final typecheck and desktop/mobile browser checks pass. Formatting, lint and tracked-file privacy checks pass with the prior optional-image warnings; full-suite/build results below predate this correction. Independent Spec passed after correcting dropped extra races; Standards found no documented violations and one optional compact mapping-duplication heuristic, deferred as non-blocking for UAT. Detailed frozen identities, reviews and private captures are recorded in the playground's `docs/rebuild/login-season-uat.md`. No commit, PR, merge or deployment.

`feat/login-season-increment` from `b93f5c9` implements the approved login and Season directions together. Protected Season reads all available selected-year records, with participation pulse, classified-finisher context, reviewed observations and evidence. Existing authentication actions are preserved. Optional local artwork, real-data fixtures and captures remain ignored/local.

Fresh Standards and Spec reviews identified and corrected scope, state, layout and regression defects; final source/capture review has no blocking findings. Production build and23 affected tests pass;85 local-corpus tests pass. The full public run passed1145 tests with two failures: the pulse's width-literal assertion was replaced by actual browser geometry verification and its affected test passes; the remaining installed-hook assertion is specific to this worktree's path while the existing hook remains installed. Formatting/privacy checks pass; lint has two optional-image warnings. Brand comparison has pre-existing upstream drift identical at base/current, not a passing alignment result.

Private desktop/mobile UAT confirms authentication/current-season entry, full selected-year data,32-column desktop scan, mobile keyboard/sticky-label behavior,44px season controls, legends/threads, year/Back and evidence. Exact private logs, manifests and captures are recorded in the orchestrator's playground under `docs/rebuild/login-season-uat.md`. Owner implementation acceptance is pending before later pages. No commit, PR, merge, release, provider setup or real email was performed. The previous full-POC-before-integration sequence was superseded by the owner's explicit incremental instruction.

### TopNav delivery checkpoint — 2026-09-09

Login + Season checkpoint `1acbf3d` preserves the previously reviewed implementation. Explicit staging included all new source/tests; privacy guard passed across 363 tracked files and the installed commit hook ran. The base remains fetched `dev` at `3292d95`.

A bounded development agent implemented the accepted branded masthead and upper-left QA navigation. Existing season selection, conditional club switching, club operations and sign-out remain under the same authenticated layout/club authority. Report tabs retain pending Race/Squad/Rider states. Disabling the temporary panel renders the required controls in the masthead. No Season visualization, schema, migration, ingestion or hosting changes belong to this navigation increment.

At this checkpoint, typecheck, formatting and nine focused navigation/selector tests pass. The public suite passed 1,158 tests with one known worktree hook-path assertion failure, 16 skips and one TODO; the configured hook points to the main checkout and remains active. New-test lint errors were corrected; final lint, browser verification and independent Standards/Spec review are in progress. Brand comparison skipped because the temporary worktree has no sibling brand checkout; prior upstream drift remains recorded. Prior production/local-corpus evidence above is historical. The endpoint requested in this packet is a reviewed pushed PR to `dev`; #146 remains open until landing and cleanup. No merge or production release is claimed.
